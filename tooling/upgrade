#!/usr/bin/env bash

command -v bun >/dev/null 2>&1 || { echo "upgrade: bun not found, unable to run upgrade" >&2; exit 1; }
tooling_dir=$(cd -- "$(dirname -- "$0")" && pwd) || exit 1
exec bun --config=/dev/null --no-env-file --no-install "$tooling_dir/upgrade-cli.ts" "$@"
