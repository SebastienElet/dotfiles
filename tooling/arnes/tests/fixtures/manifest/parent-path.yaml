version: 1
agents:
  - id: claude
    scopes: [user]
resources:
  - id: instructions
    kind: instructions
    agent: claude
    scope: user
    source: { root: repository, path: harness/AGENTS.md }
    destination: { root: home, path: ../CLAUDE.md }
