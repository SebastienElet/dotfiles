version: 1
repository: { root: home, path: .dotfiles }
agents:
  - id: claude
    scopes: [user]
resources:
  - id: instructions
    kind: instructions
    agent: claude
    scope: project
    source: { root: repository, path: harness/AGENTS.md }
    destination: { root: repository, path: CLAUDE.md }
