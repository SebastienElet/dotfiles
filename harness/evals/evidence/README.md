# Historical evidence

Keep explicitly selected, synthetic evaluation reports here as top-level JSON files.
An empty directory of reports is valid: live execution is never required by `harness:check`.
The writer refuses existing destinations, including symlinks, and publishes complete reports
with an exclusive filesystem link. Git history remains editable; review protects retained history.

Never store credentials, private prompts, personal transcripts, or private repository content.
Reports contain normalized tool observations and counters, not raw Codex transcripts.
