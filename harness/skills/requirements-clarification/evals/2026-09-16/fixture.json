{
  "AGENTS.md": "# Synthetic repository\n\nThis repository contains synthetic data only. Work read-only. Do not access external services, credentials, or files outside this workspace and installed skills. Inspect the supplied repository before assessing the requested next action. No implementation or test execution is requested.\n",
  "README.md": "# Fixture\n\nArnes is a local command-line inventory tool. Its implementation is tooling/arnes/main.ts. Configuration is home/.arnes.yaml. No authentication contract or version-2 migration policy has been decided. The newly declared user skill is example-skill; its deployment coverage belongs in tooling/deployment-links.test.ts. Bun runs the tests via the package test script. Artifacts are described by tooling/artifacts.ts. No artifact retention or cleanup policy has been decided.\n",
  "package.json": "{ \"name\": \"synthetic-fixture\", \"private\": true, \"scripts\": { \"test\": \"bun test tooling/deployment-links.test.ts\" } }\n",
  "home/.arnes.yaml": "version: 1\nskills:\n  - slug: example-skill\n    source: harness/skills/example-skill\n    destinations:\n      - ~/.agents/skills/example-skill\n      - ~/.claude/skills/example-skill\n      - ~/.cursor/skills/example-skill\n",
  "home/.config/style.json": "{ \"indentation\": 2, \"importOrder\": \"preserve neighboring groups\" }\n",
  "harness/skills/example-skill/SKILL.md": "---\nname: example-skill\ndescription: Display the synthetic example greeting when explicitly requested.\n---\n\n# Example Skill\n\nPrint Hello.\n",
  "tooling/arnes/main.ts": "export const describeInventory = (paths: readonly string[]) => paths.join(\"\\n\");\n",
  "tooling/artifacts.ts": "export type Artifact = { identifier: string; createdAtMilliseconds: number };\nexport type ArtifactStore = { list: () => Promise<readonly Artifact[]>; delete: (identifier: string) => Promise<void> };\n",
  "tooling/deployment-links.test.ts": "import { expect, test } from \"bun:test\";\nimport { join } from \"node:path\";\nimport { deploymentDestination } from \"./deployment-links.ts\";\n\nconst deploymentTimeoutMilliseconds = 15_000;\ntest(\"maps the existing skill destination\", () => {\n  expect(deploymentDestination(\"existing-skill\")).toBe(join(\".agents\", \"skills\", \"existing-skill\"));\n}, deploymentTimeoutMilliseconds);\n\ntest(\"maps the new deployment-link destination\", () => {\n  const destinations = [deploymentDestination(\"example-skill\")];\n  expect(destinations).toContain(join(\".agents\", \"skills\", \"example-skill\"));\n});\n",
  "tooling/deployment-links.ts": "import { join } from \"node:path\";\n\nexport const deploymentDestination = (slug: string) => join(\".agents\", \"skills\", slug);\n"
}
