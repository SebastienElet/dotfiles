name: Smoke tests
on: [push]
jobs:
  build:
    runs-on: macOS-latest
    timeout-minutes: 15
    steps:
    - uses: actions/checkout@v1
    - name: Execute full install
      run: make all SKIP_PAID_APPS=1
