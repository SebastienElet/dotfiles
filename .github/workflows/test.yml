name: Smoke tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  minimal:
    name: Minimal macOS profile
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - run: make smoke-minimal
